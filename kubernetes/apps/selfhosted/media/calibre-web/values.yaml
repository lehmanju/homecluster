controllers:
  calibre-web:
    containers:
      app:
        image:
          repository: ghcr.io/bjw-s-labs/calibre-web
          tag: 0.6.25@sha256:19478238942ce983e0589b887f2042ef2e723c8958978d2026bfeae6eba8eca5
        env:
          CACHE_DIR: /cache
        probes:
          liveness:
            enabled: true
          readiness:
            enabled: true
        resources:
          requests:
            cpu: 5m
            memory: 470M
          limits:
            memory: 470M
    pod:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"
service:
  app:
    controller: calibre-web
    ipFamilyPolicy: PreferDualStack
    ports:
      http:
        port: 8083
route:
  app:
    hostnames: ["books.devpi.de"]
    parentRefs:
      - name: web-internal
        namespace: cilium
        sectionName: https
persistence:
  cache:
    type: emptyDir
    globalMounts:
      - path: /cache
  config:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 5Gi
    storageClass: longhorn-backup
  media:
    existingClaim: media
    globalMounts:
      - path: /media/Books
        subPath: Books
